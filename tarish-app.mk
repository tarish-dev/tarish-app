# Tarish app — inherited by the device makefile; see docs/GRAPHENEOS.md.
# Kept separate from the daemon's tarish.mk so a build can take the
# daemon without the app, which is the arrangement used while the app was empty.
PRODUCT_PACKAGES += TarishApp
